---
import { url } from "@/lib/url";
import { SYSTEMS } from '@/config/site';
import SystemArt from '@/components/SystemArt.astro';

interface Props {
  system: string;
}

const { system } = Astro.props;

const sys = SYSTEMS[system];
const cssClass = sys?.cssKey ?? system;
const name = sys?.label ?? system;
const description = sys?.description ?? '';
const linkText = sys?.introLinkText ?? 'Browse →';
const href = sys?.route ?? `/${system}/`;
---
<div class:list={['intro-card', cssClass]}>
  <a href={url(href)} class="intro-card-main-link" data-astro-prefetch="hover">
    <div class="intro-card-art" aria-hidden="true">
    <SystemArt system={system} />
  </div>
  <div class="intro-card-body">
    <div class="intro-card-name">{name}</div>
    <div class="intro-card-desc">{description}</div>
    </div>
  </a>
  <div class="intro-card-footer">
    <a href={url(sys?.buildHref ?? href)} class="intro-card-link" data-astro-prefetch="hover">{linkText}</a>
  </div>
</div>
