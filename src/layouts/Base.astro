---
import { url } from "@/lib/url";
import { ClientRouter } from 'astro:transitions';
import SVGSprite from '@/components/SVGSprite.astro';
import "@/styles/global.css";
import "@fontsource/cinzel/400.css";
import "@fontsource/cinzel/600.css";
import "@fontsource/cinzel/700.css";
import "@fontsource/crimson-text/400.css";
import "@fontsource/crimson-text/400-italic.css";
import "@fontsource/crimson-text/600.css";
import "@fontsource/crimson-text/600-italic.css";

import cinzel700 from '@fontsource/cinzel/files/cinzel-latin-700-normal.woff2?url';
import crimson400 from '@fontsource/crimson-text/files/crimson-text-latin-400-normal.woff2?url';
import crimson400i from '@fontsource/crimson-text/files/crimson-text-latin-400-italic.woff2?url';
import crimson600 from '@fontsource/crimson-text/files/crimson-text-latin-600-normal.woff2?url';
import crimson600i from '@fontsource/crimson-text/files/crimson-text-latin-600-italic.woff2?url';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "Spheres of Power Wiki",
  description = "A quick reference site for the Spheres system.",
} = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={url("/favicon.svg")} />
    <link rel="shortcut icon" href={url("/favicon.ico")} />
    <meta name="description" content={description} />
    <meta http-equiv="X-Content-Type-Options" content="nosniff" />
    <meta name="referrer" content="strict-origin-when-cross-origin" />
    <link rel="preload" href={cinzel700} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={crimson400} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={crimson400i} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={crimson600} as="font" type="font/woff2" crossorigin />
    <link rel="preload" href={crimson600i} as="font" type="font/woff2" crossorigin />
    <title>{title}</title>
    <ClientRouter />
    <style is:inline>
      .scroll-restoring { opacity: 0; }
    </style>
    <script is:inline>
      if ('scrollRestoration' in history) history.scrollRestoration = 'manual';
      if (sessionStorage.getItem('scroll-y')) {
        document.documentElement.classList.add('scroll-restoring');
      }
    </script>
  </head>
  <body>
    <SVGSprite />
    <slot />
  </body>
  <script>
    const saved = sessionStorage.getItem('scroll-y');
    if (saved) {
      window.scrollTo(0, +saved);
      document.documentElement.classList.remove('scroll-restoring');
    }

    window.addEventListener('scroll', () => {
      sessionStorage.setItem('scroll-y', String(window.scrollY));
    }, { passive: true });
  </script>
</html>
