---
import WikiPage from '@/layouts/WikiPage.astro';
import { resolveEntries } from '@/lib/resolveEntries';

const { sphereMap, classMap } = await resolveEntries();

const spheres = [...sphereMap.values()]
  .filter(s => s.namespace === 'power')
  .sort((a, b) => a.name.localeCompare(b.name));

const classes = [...classMap.values()]
  .filter(c => c.namespace === 'power')
  .sort((a, b) => a.name.localeCompare(b.name));
---
<WikiPage title="Spheres of Power — Spheres Wiki" activeNamespace="power">

  <div class="page-title power">Spheres of Power</div>
  <hr class="page-title-rule" />

  <div class="section-heading">
    <span class="section-heading-text">Magic Spheres</span>
    <div class="section-heading-rule"></div>
  </div>

  <div class="entry-grid">
    {spheres.map(s => (
      <a href={`/spheres-of-power/${s.id}/`} class="entry-card power" data-astro-prefetch="hover">
        <div class="entry-card-header">
          <svg class="si entry-card-icon" aria-hidden="true"><use href={`#si-${s.icon}`} /></svg>
          <span class="entry-card-name">{s.name}</span>
        </div>
        {/* description from GFM body — wired in R1-6 */}
      </a>
    ))}
  </div>

  {classes.length > 0 && (
    <div style="margin-top: 20px;">
      <div class="section-heading">
        <span class="section-heading-text">Spherecaster Classes</span>
        <div class="section-heading-rule"></div>
      </div>
      <div class="entry-grid">
        {classes.map(c => (
          <a href={`/spheres-of-power/${c.id}/`} class="entry-card power" data-astro-prefetch="hover">
            <div class="entry-card-header">
              <span class="entry-card-name">{c.name}</span>
            </div>
            {/* description from GFM body — wired in R1-6 */}
          </a>
        ))}
      </div>
    </div>
  )}

</WikiPage>
